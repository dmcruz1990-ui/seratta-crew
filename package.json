{
  "name": "seratta-crew",
  "version": "1.0.0",
  "description": "Seratta Crew - Plataforma de gestión de personal",
  "main": "index.js",
  "scripts": {
    "build": "cp index.html www/index.html",
    "sync": "npm run build && npx cap sync",
    "cap:android": "npx cap open android",
    "cap:ios": "npx cap open ios"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
